import React from 'react';
import {Table} from '@material-ui/core'

export default funtion useTable (records, headCells){
  const TbtContainer = props =>{
    <Table>

    </Table>
  }
  return{
    TbtContainer
  }
}